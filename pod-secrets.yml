---
apiVersion: v1
kind: Pod
metadata:
  name: pod5

spec:
  containers:
    - name: cont1
      image: nginx
      ports:
        - containerPort: 80
      
      envFrom:
        - secretRef:
            name: secret1

